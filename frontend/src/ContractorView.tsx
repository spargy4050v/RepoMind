import { useEffect,useState } from "react";
import { ArrowLeft } from "lucide-react";
import { getContractor,type ContractorDetail } from "./api"; import { Badge } from "./components/ui/badge"; import { Button } from "./components/ui/button"; import { Card,CardContent,CardHeader } from "./components/ui/card"; import { RISK_TIERS,riskTier } from "./risk";
export function ContractorView({contractorId,onBack,onProject}:{contractorId:string;onBack:()=>void;onProject:(id:string)=>void}){const[c,setC]=useState<ContractorDetail|null>(null);useEffect(()=>{void getContractor(contractorId).then(setC)},[contractorId]);return <main className="min-h-screen bg-slate-50 text-slate-900"><header className="border-b border-slate-200 bg-white"><div className="mx-auto flex max-w-6xl items-center gap-4 px-4 py-4 sm:px-6"><Button variant="ghost" onClick={onBack}><ArrowLeft className="size-4"/>Dashboard</Button><p className="text-lg font-semibold tracking-tight">MPLAD Trace</p></div></header><div className="mx-auto max-w-6xl px-4 py-8 sm:px-6">{!c?<p className="text-sm text-slate-600">Loading contractor portfolio…</p>:<><div className="flex flex-wrap items-start justify-between gap-4"><div><h1 className="font-mono text-2xl font-semibold">{c.contractor_id}</h1><p className="mt-1 text-sm text-slate-600">Synthetic Tier 2 portfolio. Review signals are not findings of wrongdoing.</p></div><Badge variant={RISK_TIERS[riskTier(c.avg_risk_score)].variant}>Average risk {c.avg_risk_score.toFixed(0)}</Badge></div><dl className="mt-8 grid divide-y divide-slate-200 border-y border-slate-200 sm:grid-cols-3 sm:divide-x sm:divide-y-0">{[["Projects awarded",c.total_projects],["Flagged for review",c.flagged_project_count],["Constituencies",c.constituencies.length]].map(([l,v])=><div key={String(l)} className="py-4 sm:px-5"><dt className="text-sm text-slate-500">{l}</dt><dd className="mt-1 text-2xl font-semibold">{v}</dd></div>)}</dl><div className="mt-8 grid gap-6 lg:grid-cols-[1fr_2fr]"><Card><CardHeader><h2 className="font-semibold">Award footprint</h2></CardHeader><CardContent className="flex flex-wrap gap-2">{c.constituencies.map(x=><Badge key={x} variant="info">{x}</Badge>)}</CardContent></Card><Card><CardHeader><h2 className="font-semibold">Projects in this cluster</h2></CardHeader><CardContent className="grid gap-2 sm:grid-cols-2">{c.projects.map(x=><Button key={x} variant="outline" className="justify-start font-mono" onClick={()=>onProject(x)}>{x}</Button>)}</CardContent></Card></div></>}</div></main>}
